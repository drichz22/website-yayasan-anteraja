---
const { show = true } = Astro.props;
---

{show && (
  <div id="loader" class="fixed inset-0 flex items-center justify-center bg-black/70 z-50">
    <div class="h-12 w-12 border-4 border-brand-primary border-t-transparent rounded-full animate-spin"></div>
  </div>
)}
